name: Deploy Full GitOps Stack (ArgoCD + Monitoring)

on:
  push:
    branches:
      - main
  workflow_dispatch:

env:
  AWS_REGION: ${{ secrets.AWS_REGION }}
  CLUSTER_NAME: private-eks-cluster

jobs:
  deploy:
    name: Deploy via Bastion Runner
    runs-on: [aws-ssm-runner]

    steps:
      # 🔹 1. Checkout repo
      - name: Checkout repo
        uses: actions/checkout@v4

      # 🔹 2. Verify AWS identity
      - name: Verify AWS Identity
        run: aws sts get-caller-identity

      # 🔹 3. Configure kubeconfig
      - name: Configure kubeconfig
        run: |
          aws eks update-kubeconfig \
            --region $AWS_REGION \
            --name $CLUSTER_NAME

      # 🔹 4. Ensure kubectl
      - name: Ensure kubectl installed
        run: |
          if ! command -v kubectl &> /dev/null; then
            curl -LO "https://dl.k8s.io/release/$(curl -s https://dl.k8s.io/release/stable.txt)/bin/linux/amd64/kubectl"
            chmod +x kubectl
            sudo mv kubectl /usr/local/bin/
          fi

      # 🔹 5. Ensure Helm
      - name: Ensure Helm installed
        run: |
          if ! command -v helm &> /dev/null; then
            curl https://raw.githubusercontent.com/helm/helm/main/scripts/get-helm-3 | bash
          fi

      # 🔹 6. Create namespaces
      - name: Create namespaces
        run: |
          kubectl create namespace argocd --dry-run=client -o yaml | kubectl apply -f -
          kubectl create namespace monitoring --dry-run=client -o yaml | kubectl apply -f -
          kubectl create namespace online-boutique --dry-run=client -o yaml | kubectl apply -f -

      # 🔹 7. Install ArgoCD
      - name: Install ArgoCD
        run: |
          helm repo add argo https://argoproj.github.io/argo-helm
          helm repo update
          helm upgrade --install argocd argo/argo-cd \
            --namespace argocd \
            --set server.service.type=LoadBalancer

      # 🔹 8. Wait for ArgoCD
      - name: Wait for ArgoCD pods
        run: |
          for deploy in $(kubectl get deploy -n argocd -o jsonpath='{.items[*].metadata.name}'); do
            kubectl rollout status deployment/$deploy -n argocd --timeout=120s
          done

      # 🔹 9. Wait for CRDs
      - name: Wait for CRDs
        run: |
          kubectl wait --for condition=Established \
            crd/applications.argoproj.io \
            --timeout=120s

      # 🔹 10. Checkout GitOps repo
      - name: Checkout GitOps repo
        uses: actions/checkout@v4
        with:
          repository: ${{ vars.DEPLOYMENT_REPO }}
          ref: ${{ vars.DEPLOYMENT_REPO_BRANCH }}
          token: ${{ secrets.GITOPS_TOKEN }}
          path: gitops-repo

      # 🔹 11. Apply ArgoCD Applications (ALL)
      - name: Apply All ArgoCD Apps
        run: |
          kubectl apply -f gitops-repo/prometheus.yaml -n argocd
          kubectl apply -f gitops-repo/grafana.yaml -n argocd
          kubectl apply -f gitops-repo/application.yaml -n argocd

      # 🔹 12. Apply ServiceMonitor
      - name: Apply ServiceMonitor
        run: |
          kubectl apply -f gitops-repo/argocd-servicemonitor.yaml

      # 🔹 13. Verify Applications
      - name: Verify ArgoCD Apps
        run: |
          kubectl get applications -n argocd

      # 🔹 14. Get ArgoCD Service
      - name: Get ArgoCD Service
        run: |
          kubectl get svc -n argocd